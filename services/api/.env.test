# Committed on purpose - these are dummy values that let unit tests import
# config.js (which fails fast on a missing RABBITMQ_URL by design) without
# needing real credentials. Never used for anything but running tests.
RABBITMQ_URL=amqps://test:test@test.example.com/vhost
